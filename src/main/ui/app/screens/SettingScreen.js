import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View, TextInput, Button } from 'react-native';

export default function SettingScreen() {
    return (
        <View>
            <Text>Les paramètres</Text>
        </View>
    );
}
